# see https://symfony.com/doc/current/reference/configuration/framework.html
parameters:
    app.runtime_secret_default: 'local-development-secret-change-in-deployment'

framework:
    secret: '%env(default:app.runtime_secret_default:APP_SECRET)%'

    # Note that the session will be started ONLY if you read or write from it.
    session: true

    #esi: true
    #fragments: true

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file
