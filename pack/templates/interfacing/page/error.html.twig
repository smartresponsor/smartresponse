{# Interfacing error page (RVE-A4)
   - unified error semantics for UI
   - do not leak domain details
#}

{% extends 'interfacing/base.html.twig' %}

{% block body %}
  <div class="interface-error">
    <h2 class="interface-error__title">{{ title }}</h2>
    <div class="interface-error__meta">
      <span class="interface-error__status">HTTP {{ status }}</span>
      <span class="interface-error__code">{{ code }}</span>
    </div>
    <p class="interface-error__detail">{{ detail }}</p>
    {% if traceId is not empty %}
      <p class="interface-error__trace">Trace: {{ traceId }}</p>
    {% endif %}
    <div class="interface-error__action">
      <a href="/interfacing" class="interface-link">Back to Interfacing</a>
    </div>
  </div>
{% endblock %}
