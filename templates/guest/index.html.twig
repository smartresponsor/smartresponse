{# Copyright (c) 2025 Oleksandr Tishchenko / Marketing America Corp #}
{#
  Canonical guest storefront shim.

  The host renderer asks for `guest/index`, so keep this local template in
  the app root and delegate the actual layout to the shared Interfacing surface.
#}
{% set workbench = {
  title: title|default('SmartResponsor'),
  routeContext: routeContext|default({
    resourcePath: 'guest-home',
    resourceLabel: 'SmartResponsor',
    resourceCollectionLabel: 'Guest storefront',
    operation: 'overview',
    surface: 'storefront',
    mode: 'collection',
    collectionHref: '/'
  }),
  paginationLabel: paginationLabel|default('Guest storefront rendered through the shared Interfacing shell.'),
  rows: rows|default([]),
  columns: columns|default([]),
  filters: filters|default([]),
  headerActions: headerActions|default([])
} %}
{% extends '@Interfacing/application/dashboard.html.twig' %}
