{# Copyright (c) 2025 Oleksandr Tishchenko / Marketing America Corp #}
{#
  Canonical host dashboard shim.

  The host renderer asks for `dashboard/index`, so keep this local template in
  the app root and delegate the actual layout to the shared Interfacing surface.
#}
{% set workbench = {
  title: title|default('Commerce Control Center'),
  routeContext: routeContext|default({
    resourcePath: 'app-dashboard',
    resourceLabel: 'Commerce Control Center',
    resourceCollectionLabel: 'Application dashboard',
    operation: 'overview',
    surface: 'dashboard',
    mode: 'collection',
    collectionHref: '/'
  }),
  paginationLabel: paginationLabel|default('App-owned dashboard sections rendered through the shared Interfacing shell.'),
  rows: rows|default([]),
  columns: columns|default([]),
  filters: filters|default([]),
  headerActions: headerActions|default([])
} %}
{% extends '@Interfacing/dashboard/index.html.twig' %}
